# video-call-service

LiveKit token issuance and the live video call for the AI technical round: the candidate joins,
an AI voice layer (STT/TTS) asks questions core-api generated from the job description and
resume, and relays each response back to core-api as the call goes.

Full architecture: see the `platform` repo's README (sibling folder).

Not built yet.
